openapi: 3.0.3
info: {title: Gemara, version: v1.0.0}
components:
  schemas:
    ControlEvaluation:
      type: object
      properties:
        name: {type: string}
        result: {type: string, enum: ["Passed", "Failed", "Unknown"]}
        message: {type: string}
        severity: {type: string}
      required: [name, result, message, severity]
